import React from 'react';
import * as styled from './styled';

export default function AuthSection({ children }){
    return(
        <styled.AuthSection>
            {children}
        </styled.AuthSection>
    )
}